name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate registry.json
        run: python3 -c "import json, sys; json.load(open('registry.json')); print('registry.json is valid')"

      - name: Check required files exist
        run: |
          files=(
            "kingdee-k3cloud/SKILL.md"
            "kingdee-k3cloud/references/verified-fields.md"
            "kingdee-k3cloud/references/common-errors.md"
          )
          for f in "${files[@]}"; do
            [ -f "$f" ] && echo "✓ $f" || { echo "✗ missing: $f"; exit 1; }
          done

      - name: Validate registry.json path field
        run: |
          path=$(python3 -c "import json; print(json.load(open('registry.json'))['path'])")
          [ -d "$path" ] && echo "✓ registry.json path '$path' exists" || { echo "✗ registry.json path '$path' not found"; exit 1; }
